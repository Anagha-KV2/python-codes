class ListNode:
    def__init__(self, val=0)
        self.val = val
        self.next = None

        def insert_beginning(head, value):
            new_node = ListNode(value)



            def insert_end(head,value)
                new_node=ListNode(value)
                if head is None:
                    return new_node
                    current=head
                    while current.(variable) current: Any
                    current = current.next

                    def print_list(head)
                        current = head
                        while current:
                            print(current.val, end=" -> ")
                            current = current.next
                            print("None")


